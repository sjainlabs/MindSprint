import { type Routes } from '@angular/router';
import { DiagnosticTestComponent } from '../../components/diagnostic-test/diagnostic-test';

export const diagnosticRoutes: Routes = [{ path: '', component: DiagnosticTestComponent }];
